#! /sr/bin/env bash

#	Package config for a host with rsync
##	rsync -av ...

#	Create a commit to the `deploy` branch with Commit message `Deployment of $HOST @ $UNIXTIME - automated script`
##	git ...

#	Unpack the config and install it.
##	tar -xzf ...
