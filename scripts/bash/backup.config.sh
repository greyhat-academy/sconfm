#! /sr/bin/env bash

#	Backup existing config from a host with rsync
##	rsync -av ...

#	Create a commit to the `backup` branch with Commit message `Backup of $HOST @ $UNIXTIME - automated script`
##	git ...

